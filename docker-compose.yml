services:
  ha-mcp-server:
    build: .
    container_name: ha-mcp-server
    ports:
      - "${MCP_PORT:-8080}:8080"
    environment:
      HA_URL: ${HA_URL}
      HA_TOKEN: ${HA_TOKEN}
      FASTMCP_HOST: "0.0.0.0"
      FASTMCP_PORT: "8080"
    restart: unless-stopped
